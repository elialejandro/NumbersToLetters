language: php

sudo: false

matrix:
    include:
        - php: 5.3.3
        - php: 5.4
        - php: 5.5
        - php: 5.6
        - php: 7

before_script:
  - composer install -o

script:
  - (cd tests ; phpunit -c phpunit.xml)